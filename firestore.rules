rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write during development for all collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
